let size = arr.length;

arr[2] = "Jeevan";
arr.push("Kishan");
arr.push(-1); // adds the element in the end
arr.push(0);
arr.pop(); // remove the element from the end

arr.unshift(0); // adds the element in beginning
arr.unshift(3);
arr.shift(); // removes the element from beginning

console.log(arr);
// create an array
//adds your skills by using push method atleast 4
// pop
// adds 2 intrests of yours by unshift
// unshift the array

let isPresent = arr.includes("2");
let pos = arr.lastIndexOf("A");
